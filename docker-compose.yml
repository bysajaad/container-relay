services:
  relay:
    build: .
    environment:
      LISTEN_PORT: ${LISTEN_PORT:-8080}
      TARGET_HOST: ${TARGET_HOST}
      TARGET_PORT: ${TARGET_PORT:-${LISTEN_PORT:-8080}}
    ports:
      - "443:443"
    restart: unless-stopped
